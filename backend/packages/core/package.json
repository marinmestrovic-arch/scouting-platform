{
  "name": "@scouting-platform/core",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@prisma/client": "^7.0.0",
    "@scouting-platform/contracts": "workspace:*",
    "@scouting-platform/db": "workspace:*",
    "@scouting-platform/integrations": "workspace:*",
    "argon2": "^0.43.0",
    "csv-parse": "^6.1.0",
    "pg-boss": "^12.14.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests --fileParallelism=false",
    "typecheck": "tsc --noEmit"
  }
}
